# qiyom — docker-compose root env (.env).
# Nusxa oling:  cp .env.template .env
# APP_KEY generatsiya:  docker compose run --rm api php artisan key:generate --show

APP_NAME=qiyom
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8001
APP_TIMEZONE=Asia/Tashkent

# PostgreSQL (docker db servisi ichida host=db, port=5432; tashqi port 5433)
DB_DATABASE=qiyom
DB_USERNAME=qiyom_user
DB_PASSWORD=qiyom_secret
